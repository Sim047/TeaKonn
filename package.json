{
  "name": "teakonn",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "install": "cd backend && npm install",
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

# Example env for TEAKONN backend
# Configure these in Railway (or your host) — do NOT commit real secrets

MONGO_URI=mongodb+srv://<user>:<pw>@cluster.example.mongodb.net/teakonn
JWT_SECRET=replace_with_a_secure_random_string
FRONTEND_URL=https://your-frontend.example.com
UPLOAD_DIR=uploads

# Cloudinary Configuration (for persistent image storage)
# Sign up at https://cloudinary.com (free tier available)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret
